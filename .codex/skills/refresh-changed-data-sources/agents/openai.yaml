interface:
  display_name: "Changed Data Source Refresh"
  short_description: "Rebuild only changed local data sources"
  default_prompt: "Use $refresh-changed-data-sources to detect changed data source files and run drop, init, and dummy only for those local services."
